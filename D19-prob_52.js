function between(a, b) {
    let arr = []
    for (let i = a; i <= b; i++) {
        arr.push(i);        
    }
    return arr;
  }
const between  = (a,b) => {
    let arr = []
    for (let i = a; i <= b; i++) {
        arr.push(i);        
    }
    return arr;
}
  between(1,4)
  between(-2,2)
